#include "Reservation.h"
#include <string>
using namespace std;

Reservation::Reservation(int code, string passenger, string flight, int reservationDate, double reservationPrice) :
Reservation code(code), passenger(passenger), flight(flight), reservationDate(reservationDate), reservationPrice(reservationPrice) {}
void Reservation::reservation() {

}
void Reservation::calculateReservationPrice() {

}